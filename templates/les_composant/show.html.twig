{% extends 'base.html.twig' %}

{% block title %}Composant{% endblock %}

{% block body %}
    <h1>Composant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ composant.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ composant.label }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ composant.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_les_composant_index') }}">back to list</a>

    <a href="{{ path('app_les_composant_edit', {'id': composant.id}) }}">edit</a>

    {{ include('les_composant/_delete_form.html.twig') }}
{% endblock %}
