{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ voiture.model }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ voiture.prix }}</td>
            </tr>
            <tr>
                <th>Archive</th>
                <td>{{ voiture.archive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_liste_voiture_index') }}">back to list</a>

    <a href="{{ path('app_liste_voiture_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('liste_voiture/_delete_form.html.twig') }}
{% endblock %}
